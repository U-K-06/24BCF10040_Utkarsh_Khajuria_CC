class Solution {
public:
    bool isValid(string s) 
    {
        stack<char> s1;
        unordered_map<char,char> mapping = {
            {'(',')'},
            {'[',']'},
            {'{','}'}
        };
        for(char c: s)
        {
            if(mapping.containsd(c))
            {
                s1.push(i);
            }
            if(!s1.isempty() &&  )
        }
    }
};